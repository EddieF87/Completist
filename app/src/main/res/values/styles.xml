<resources>

    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimaryDark</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:textColor">@color/colorAccent</item>
        <item name="android:windowBackground">@color/colorPrimaryDark</item>
        <item name="android:colorBackground">@color/colorPrimaryDark</item>
    </style>

    <style name="TransparentText" parent="@android:style/TextAppearance">
        <item name="android:textColor">#00000000</item>
    </style>

    <style name="CollapsedText" parent="@android:style/TextAppearance">
        <item name="android:textColor">@color/colorAccent</item>
        <item name="android:textSize">@dimen/titleTextSize</item>
        <item name="android:textStyle">bold</item>
    </style>

    <style name="ListButtonStyle">
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_weight">1</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_gravity">center</item>
        <item name="android:gravity">center</item>
-        <item name="android:background">@drawable/btn_selector</item>
        <item name="android:button">@android:color/transparent</item>
        <item name="android:textColor">@color/colorAccent</item>
        <item name="android:textSize">@dimen/btnTextSize</item>
        <item name="android:textStyle">bold</item>
        <item name="android:padding">@dimen/smallPadding</item>
        <item name="android:layout_marginStart">@dimen/defaultMargin</item>
        <item name="android:layout_marginEnd">@dimen/defaultMargin</item>
    </style>

    <style name="ListButtonStyleLandscape" parent="ListButtonStyle">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_marginBottom">@dimen/defaultBtnPadding</item>
        <item name="android:layout_marginTop">@dimen/defaultMargin</item>
        <item name="android:layout_marginStart">@dimen/defaultMargin</item>
        <item name="android:layout_marginEnd">@dimen/defaultBtnPadding</item>
        <item name="android:padding">@dimen/defaultBtnPadding</item>
    </style>
</resources>